const duration = {
  default: 300,
  slow: 400,
};

const speed = {
  fast: 50,
  slow: 20,
};

const notificationVisibleTiming = 5000;

const animation = {
  duration,
  speed,
  notificationVisibleTiming,
};

export default animation;
