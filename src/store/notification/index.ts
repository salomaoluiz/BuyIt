import actions from './actions';
import * as selector from './selectors';

const notificationActions = { ...actions };
const notificationSelector = { ...selector };

export { notificationActions, notificationSelector };
