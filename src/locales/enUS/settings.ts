const settings = {
  settings: `Settings`,
  language: `English`,
  selectTheLanguage: `Select the language`,
  restartTheApp: `Restart the app`,
  willRestartNow: `To apply the changes we need restart the app, want restart now?`,
};

export default settings;
